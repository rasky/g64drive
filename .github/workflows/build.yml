name: build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  Linux-Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18.x
      - name: Build
        run: go build --tags 'netgo osusergo' -ldflags '-s -w -extldflags "-static -ludev"'
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: g64drive-linux-amd64
          path: g64drive
